version: '3.8'
services:
  backend:
    image: thu000acr.azurecr.io/rag-backend:latest
    ports:
      - "8000:8000"
    restart: always

  frontend:
    image: thu000acr.azurecr.io/rag-frontend:latest
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always