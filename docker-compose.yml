version: "3.9"

services:

  backend:
    build: ./backend
    container_name: student-rag-backend

    ports:
      - "8000:8000"

    volumes:
      - ./backend/data:/app/data
      - ./backend/chroma_db:/app/chroma_db

    env_file:
      - ./backend/.env